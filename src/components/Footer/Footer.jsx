import s from './Footer.module.css';

const Footer = () => {
  return (
    <footer className={s.footer}>
      <p className={s.developer}>Developer: David</p>
    </footer>
  )
};

export default Footer;